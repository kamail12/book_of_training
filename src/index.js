import React from "react";
import ReactDOM from "react-dom/client";
import PasswordComponent from "./components/Password";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<PasswordComponent />);
